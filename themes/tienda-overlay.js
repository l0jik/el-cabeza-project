/* Placeholder: the order form and the box lid come next. */
import React from "react";
export function useSetupExtras(x) {
  return { ...x, openOrderForm: () => {} };
}
export function renderExtraOverlays() {
  return null;
}
